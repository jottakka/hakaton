# Archived: GEO Site Audit System Prompt

This prompt powered the legacy `claude-agent-sdk` loop in
`benchmark_control_arcade.geo_runner`.

It is kept for historical reference only. Active GEO execution now calls
`geo_audit_arcade.tools.run_geo_audit.run_geo_audit()` directly.
