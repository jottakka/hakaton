# Archived: GEO Compare System Prompt

This prompt powered the legacy `claude-agent-sdk` loop in
`benchmark_control_arcade.geo_compare_runner`.

It is kept for historical reference only. Active GEO compare execution now calls
`geo_audit_arcade.tools.run_geo_compare.run_geo_compare()` directly.
